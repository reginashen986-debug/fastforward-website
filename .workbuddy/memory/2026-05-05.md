# 2026-05-05

## creative.html 视频控制按钮 + 走马灯修复
- `.vs-ctrl` 全端隐藏（`display: none !important`），手机端/桌面端均无按钮
- 走马灯不滚动根因：`initMarquee` 依赖 `scrollWidth` 但父元素 `overflow:hidden` 导致子元素 `scrollWidth` 不可靠
- 修复：改用 `offsetWidth * 卡片数 + gap` 手动计算 halfWidth，加 50ms 延迟确保布局完成，防止跳帧（dt > 0.1s 截断）
- 已推送 commit: cdc50bf
